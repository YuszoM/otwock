/** Booking UI labels aligned with oowit.pl CTAs. */
export function specialistBookingCta(_slug: string, _fallbackName: string): string {
  return "Umów konsultację";
}

export function specialistServiceHeading(_slug: string, _fallbackName: string): string {
  return "Poznaj naszą ofertę";
}
