# Supabase (post-contract)

SQL for production features deferred until after demo contract:

- `contact_leads.sql` — contact form persistence (demo uses client-side success only)
- Booking schema — per-practitioner calendars (Phase 3, deferred)

Run in Supabase SQL editor when project moves beyond presentation demo.
